# Python Microservice

FastAPI service for AI/ML tasks.

## Local Development

```bash
# Install dependencies
pip install -r requirements.txt

# Run server
python main.py

# Or with auto-reload
uvicorn main:app --reload --port 8000